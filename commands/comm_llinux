# Команда du -hac /var/log/nginx/ | sort -rh сортирует вывод по размеру в убывающем порядке. Чтобы отсортировать по возрастанию, нужно изменить параметр сортировки на -r на -n, что указывает на сортировку по числовому значению:
du -hac /var/log/nginx/ | sort -h